# v0.3.0 physical iPhone PWA session

This is the only planned user touchpoint. Begin only after the complete automated matrix and all three independent personas pass at one frozen `CERT_SHA`. Use a disposable HTTPS candidate with isolated state and secrets, never production data. Confirm `/version` and `/readyz` identify `CERT_SHA`, schema 2, and protocol 2 before testing.

Record only `PASS` or `FAIL`, the failed step ID, and a short symptom. Do not record account details, codes, tokens, game IDs, card values, screenshots of private cards, notification endpoints, or secrets.

- [ ] **I0 Identity and launch:** portrait and landscape open without browser errors and match the supplied release identity.
- [ ] **I1 Home and new setup:** with no save, Home clearly offers Start Solo Game and Multiplayer. Setup initially selects one bot and Medium Recommended; 1-7 controls and all five difficulty choices work without creating a game before Start.
- [ ] **I2 Saved-game safety:** after making progress, Home shows Continue with accurate opponent/difficulty metadata and a distinct New Solo Game action. Back, Cancel, outside tap, Escape-equivalent dismissal where available, and Keep Current Game preserve the board; confirmed replacement creates the requested setup once.
- [ ] **I3 Setup accessibility:** with VoiceOver and 200% text, read and change opponent count/difficulty, open replacement review, and return focus to the safe trigger. Labels, selected state, summaries, buttons, and protected-save warning remain understandable and reachable with no horizontal page scroll.
- [ ] **I4 Difficulty and Mixed durability:** start a three-bot Mixed game, note only the visible difficulty badges, force-close/relaunch, Continue, and verify the same badges remain attached to the same named opponents. Start a fixed Ultra Hard game and verify active settings are read-only.
- [ ] **I5 Game-over choices:** finish or use the prepared disposable near-complete game. Play again with same setup preserves opponent count/difficulty; Change setup returns to an unprotected setup and starts the newly selected configuration.
- [ ] **I6 Offline restore:** make progress, force-close, cold-launch with network disabled, Continue the exact board, then restore network. Multiplayer remains unavailable/read-only offline and no active game reload is forced.
- [ ] **I7 Rotation and safe areas:** rotate Home, setup, replacement dialog, active table, and game-over controls on the iPhone 16 Pro Max. Nothing hides behind the Dynamic Island/home indicator, overlaps, or requires horizontal page scrolling.
- [ ] **I8 Existing PWA behavior:** audio, turn notifications, background/lock recovery, and the centered multiplayer table remain functional; the discard label is not clipped.

## Defect loop

Any failure returns to its owning issue and creates a new protected-main candidate. Rerun the complete automated matrix and all personas. On device, repeat I0 plus the failed step; also repeat an adjacent step when the defect affects shared storage, routing, layout, accessibility, service worker, or reconnect behavior. Only an all-pass session authorizes immutable tagging.
